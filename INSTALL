To build on UNIX, run the build/build script. This sets up your system specific
configure tools, builds the shared library and builds all test programs

To build on Windows with Visual Studio 2008, use the build/Blister.sln
Solution file

